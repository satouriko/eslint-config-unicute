# Missing blank line before [section]      → toml/padding-line-between-tables (prettier off)
# Extra spaces around `=` and spacing drift → prettier/prettier (prettier on)
key   =    1
[section]
other  =   2
